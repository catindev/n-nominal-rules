{
  "id": "library.address.cond_region_name_if_present",
  "type": "condition",
  "description": "Если beneficiary.address.registration.regionName заполнено, проверяем формат/длину",
  "when": {
    "all": [
      "library.address.pred_region_name_present"
    ]
  },
  "steps": [
    {
      "rule": "library.address.region_name"
    }
  ]
}
