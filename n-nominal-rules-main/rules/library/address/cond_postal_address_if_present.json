{
  "id": "library.address.cond_postal_address_if_present",
  "type": "condition",
  "description": "Если почтовый адрес указан, проверяем запрет США",
  "when": {
    "all": [
      "library.address.pred_postal_address_present"
    ]
  },
  "steps": [
    {
      "rule": "library.address.postal_address_not_us"
    }
  ]
}
