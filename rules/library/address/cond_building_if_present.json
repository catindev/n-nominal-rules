{
  "id": "library.address.cond_building_if_present",
  "type": "condition",
  "description": "Если beneficiary.address.registration.building заполнено, проверяем формат/длину",
  "when": {
    "all": [
      "library.address.pred_building_present"
    ]
  },
  "steps": [
    {
      "rule": "library.address.building"
    }
  ]
}
